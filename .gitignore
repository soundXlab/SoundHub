# Python
__pycache__/
*.pyc
.venv/
backend/data/
backend/.env

# Node
node_modules/
frontend/dist/

# Misc
*.log
.DS_Store
*.tsbuildinfo

# personal conversation log — keep local
CONVERSATION_LOG.md

# local screenshot tooling (puppeteer cache + extracted browser libs)
.puppeteer-cache/
.local-libs/
.vercel

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Russian docs (local only)
*_RU.md

# OpenAPI contract artifacts (current snapshot and diff report)
openapi_current.json
OPENAPI_DIFF.md